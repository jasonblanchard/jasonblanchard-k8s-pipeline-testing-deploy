apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: notebook-ingress
  namespace: istio-system
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: istio-ingressgateway
            port:
              number: 80
